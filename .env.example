# Template — copy to `.env` and put your real key in.
# The tool also works WITHOUT a key (it falls back to a local report).
#
#   cp .env.example .env      # then edit .env
#
# Rules: filename must be exactly `.env`, no spaces around `=`, no quotes.
ANTHROPIC_API_KEY=sk-ant-...
